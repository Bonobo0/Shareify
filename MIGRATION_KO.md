# MongoDB → PostgreSQL 마이그레이션 구현 완료

## 개요
Shareify 프로젝트의 데이터베이스를 MongoDB에서 PostgreSQL 17 (Neon 호환)로 마이그레이션할 수 있도록 구현이 완료되었습니다.

## 구현된 기능

### 1. 데이터베이스 지원
- ✅ PostgreSQL 17 (Neon 호환) - 권장
- ✅ MongoDB (기존) - 레거시 지원
- ✅ 환경 변수로 자동 선택

### 2. 마이그레이션 도구
- ✅ 관리자 페이지 `/admin/migration`
- ✅ 수동 단계별 마이그레이션
- ✅ 자동 전체 마이그레이션
- ✅ 실시간 상태 추적
- ✅ 오류 처리 및 보고

### 3. 데이터베이스 모델
- ✅ User (사용자 - 2FA, 비밀번호 해싱)
- ✅ File (파일 - 암호화, 공유)
- ✅ Directory (디렉토리 - 계층 구조, 공유)
- ✅ RateLimit (속도 제한 - 자동 정리)

## 사용 방법

### 환경 설정
`.env` 파일에 다음 중 하나를 설정:

```bash
# PostgreSQL 사용 (권장)
DATABASE_URL=postgresql://user:password@host:5432/database?sslmode=require

# 또는 MongoDB 사용 (기존)
MONGODB_URI=mongodb://localhost:27017/shareify
```

### 마이그레이션 실행

#### 자동 마이그레이션 (권장)
1. 관리자로 로그인
2. `/admin/migration` 페이지 접속
3. "자동 전체 마이그레이션" 버튼 클릭
4. 완료될 때까지 대기 (대용량 DB는 수 분 소요)
5. 상태 패널에서 데이터 개수 확인

#### 수동 마이그레이션 (단계별)
1. "스키마 초기화" - PostgreSQL 테이블 생성
2. "사용자 마이그레이션" - 사용자 데이터 이전
3. "디렉토리 마이그레이션" - 디렉토리 데이터 이전
4. "파일 마이그레이션" - 파일 데이터 이전
5. "Rate Limit 마이그레이션" - Rate Limit 데이터 이전

## 주요 특징

### 안전성
- 🔄 **롤백 가능**: 문제 발생 시 MongoDB로 즉시 복귀 가능
- 🛡️ **멱등성**: 마이그레이션을 여러 번 실행해도 안전
- 📊 **상태 추적**: 양쪽 데이터베이스의 데이터 개수 실시간 확인
- ⚠️ **오류 처리**: 상세한 오류 메시지와 로그

### 호환성
- ✅ **기존 코드 유지**: 모든 DB 구조와 작업 유지
- ✅ **제로 다운타임**: 마이그레이션 중에도 서비스 가능
- ✅ **동일 API**: 기존 Mongoose API와 동일하게 사용
- ✅ **자동 전환**: 환경 변수만 변경하면 자동으로 전환

### 성능
- ⚡ **연결 풀링**: 최대 20개 연결, 효율적 관리
- 📈 **인덱싱**: 모든 중요 필드에 인덱스 자동 생성
- 🔍 **쿼리 로깅**: 느린 쿼리(>1초) 자동 로깅
- 💾 **JSONB**: 복잡한 데이터를 효율적으로 저장

## 테스트 방법

### 1. PostgreSQL 연결 테스트
```bash
# DATABASE_URL 설정 후
node src/lib/db/test-pg.js
```

### 2. 웹 UI에서 테스트
1. 관리자로 로그인
2. `/admin/migration` 접속
3. "새로고침" 버튼으로 상태 확인
4. 마이그레이션 실행

### 3. 기능 테스트
- [ ] 로그인/회원가입
- [ ] 파일 업로드/다운로드
- [ ] 디렉토리 생성/관리
- [ ] 파일 공유
- [ ] 2FA 인증
- [ ] 관리자 기능

## 문제 해결

### PostgreSQL 연결 실패
- `DATABASE_URL` 설정 확인
- PostgreSQL 서버 접근 가능 여부 확인
- 방화벽/네트워크 설정 확인

### 마이그레이션 실패
- 스키마 초기화부터 다시 시작
- MIGRATION.md 파일의 문제 해결 섹션 참조
- 로그 확인 후 에러 메시지 검토

### 데이터 불일치
- 상태 패널에서 개수 확인
- 마이그레이션 재실행 (안전함)
- 특정 테이블만 다시 마이그레이션 가능

## 롤백 방법

문제 발생 시 MongoDB로 복귀:

1. `.env`에서 `DATABASE_URL` 제거
2. `MONGODB_URI` 유지
3. 애플리케이션 재시작
4. 자동으로 MongoDB 사용

## 권장 사항

### 마이그레이션 전
1. MongoDB 백업 생성
2. 테스트 환경에서 먼저 시도
3. DATABASE_URL 설정 확인

### 마이그레이션 중
1. 진행 상황 모니터링
2. 오류 발생 시 기록
3. 데이터 개수 일치 확인

### 마이그레이션 후
1. 모든 기능 테스트
2. 성능 모니터링
3. 30일간 MongoDB 백업 유지
4. 확신이 생기면 MongoDB 제거

## 추가 문서

- **MIGRATION.md**: 상세한 마이그레이션 가이드 (영문)
- **IMPLEMENTATION.md**: 기술적 구현 세부사항 (영문)
- **README.md**: 업데이트된 프로젝트 개요

## 지원

문제가 발생하면:
1. MIGRATION.md의 문제 해결 섹션 확인
2. 애플리케이션 로그 검토
3. PostgreSQL 로그 확인
4. GitHub 이슈로 문의

## 결론

MongoDB에서 PostgreSQL로의 마이그레이션 구현이 완료되었습니다. 모든 기존 기능이 유지되며, 안전하고 단계적인 마이그레이션이 가능합니다.

**주요 장점**:
- 제로 다운타임 마이그레이션
- 기존 코드 변경 불필요
- 롤백 가능
- Neon 호환으로 비용 절감
- 더 나은 성능과 확장성

마이그레이션을 시작하려면 `/admin/migration` 페이지를 방문하세요! 🚀
